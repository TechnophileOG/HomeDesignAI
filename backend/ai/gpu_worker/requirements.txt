# KatalogitAI GPU worker — Python deps (install on the CUDA box)
# torch/torchvision are installed separately from the CUDA wheel index (see Dockerfile).
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.28
# PINNED: transformers 5.x + diffusers 0.33+ break IP-Adapter (image encoder
# returns a tuple where attention_processor expects a tensor). This exact
# combo is verified working for SD1.5 + IP-Adapter + ControlNet (2026-08).
diffusers==0.32.2
transformers==4.57.6
accelerate>=0.34
Pillow>=10.4
